CHIP Nand2DMux4Way {
    IN in, sel[2];
    OUT a, b, c, d;

    PARTS:
    DMux(in = in, sel = sel[0], a = a);
    DMux(in = in, sel = sel[1], a = b);
    DMux(in = in, sel = sel[2], a = c);
    DMux(in = in, sel = sel[3], a = d);

}
